{
  "name": "influxdb-tg-bot",
  "version": "0.0.1",
  "description": "Telegram bot that can read, create visualizations and send notifications based on InfluxDB data.",
  "author": "iiroki",
  "main": "src/index.ts",
  "scripts": {
    "start": "NODE_ENV=production node dist/index.js",
    "dev": "npm run build && NODE_ENV=development LOG_LEVEL=debug node dist/index.js",
    "build": "tsc",
    "build:prod": "tsc && npm prune --omit=dev"
  },
  "devDependencies": {
    "@tsconfig/node18": "^1.0.1",
    "@types/node": "^18.14.0",
    "typescript": "^4.9.5"
  }
}
